{
  "name": "scottfeltham.com",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run sync-static && astro dev",
    "prebuild": "npm run sync-static",
    "build": "astro build",
    "preview": "astro preview",
    "sync-static": "rm -rf public && mkdir -p public && cp -R assets public/assets && cp -R terminal public/terminal && cp assets/favicon.svg public/favicon.svg"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "github-slugger": "^2.0.0",
    "js-yaml": "^4.1.0"
  }
}
